export const objType = `type objType {
    createdAt: String,
    updatedAt: String
}`
export const Student = `type Student {
    _id: ID!,
    name: String,
    sex: String,
    age: Int,
    info: Info
}`